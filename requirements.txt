# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
pydub>=0.25.0

# Music information retrieval
# Note: madmom requires Cython and has build issues with Python 3.13
# It's optional - rhythm extraction will work without it using librosa
# If needed, install manually: pip install Cython && pip install madmom
# madmom>=0.16.1
pretty_midi>=0.2.10
music21>=9.1.0

# CLAP for audio encoding and similarity
laion-clap>=1.1.0
transformers>=4.35.0

# Audio captioning models
accelerate>=0.24.0
sentencepiece>=0.1.99

# Evaluation metrics
torchmetrics>=1.2.0
scikit-learn>=1.3.0

# Music generation models
# Note: MuseControlLite, MusicGen need to be installed separately from their repos
diffusers>=0.24.0
audiocraft>=1.0.0  # For MusicGen

# Utilities
tqdm>=4.66.0
matplotlib>=3.7.0
seaborn>=0.12.0
jupyter>=1.0.0
ipywidgets>=8.0.0

# Optional: For better audio quality
resampy>=0.4.2
